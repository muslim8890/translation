# دليل النشر السريع على Render.com ☁️🚀

بما أنك تريد التجربة السريعة والمجانية، Render هو الخيار الأفضل.

## الخطوة 1: تحضير الكود (GitHub)
موقع Render يسحب الكود من GitHub. لذا يجب عليك رفع الملفات أولاً.

1.  أنشئ حساباً على [GitHub.com](https://github.com) (إذا لم يكن لديك).
2.  أنشئ مستودعاً جديداً (New Repository) وسمّه `smart-translator`.
3.  افتح موجه الأوامر (Terminal) في مجلد المشروع `d:\app_file\translat` ونفذ الأوامر التالية:
    ```bash
    git init
    git add .
    git commit -m "Initial Deploy"
    git branch -M main
    git remote add origin https://github.com/USERNAME/smart-translator.git (استبدل الرابط برابطك)
    git push -u origin main
    ```

## الخطوة 2: الإعداد على Render
1.  سجل الدخول في [dashboard.render.com](https://dashboard.render.com).
2.  اضغط زر **New +** واختر **Web Service**.
3.  اربط حسابك بـ GitHub واختر المستودع `smart-translator`.
4.  في صفحة الإعدادات، املأ البيانات كالتالي:
    *   **Name**: `smart-translator-pro`
    *   **Region**: `Frankfurt` (أقرب ونفس توقيتنا تقريباً).
    *   **Runtime**: **Docker** (مهم جداً أن تختار Docker).
    *   **Instance Type**: `Free`.
5.  في قسم **Environment Variables** (المتغيرات البيئية)، أضف:
    *   `GEMINI_API_KEY`: (ضع مفتاحك هنا).
6.  اضغط **Create Web Service**.

## الخطوة 3: الانتظار والتشغيل
*   سيقوم Render بقراءة ملف `Dockerfile` الذي صنعته لك.
*   سيقوم بتنصيب النظام والمكتبات (قد يأخذ 5-10 دقائق في المرة الأولى).
*   عندما ينتهي، سيعطيك رابطاً مثل `https://smart-translator-pro.onrender.com`.

### ⚠️ ملاحظة هامة:
الخطة المجانية في Render تتوقف عن العمل (Sleep) إذا لم يزرها أحد لمدة 15 دقيقة. عند فتح الرابط مرة أخرى، قد يتأخر 30-50 ثانية ليعمل (Wake up). هذا طبيعي في الخطط المجانية.
