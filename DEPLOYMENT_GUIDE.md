# دليل النشر والتصدير (Deployment Guide) 🚀

## أولاً: تطبيق الويب (Web App) 🌍
لقد قمت بدمج الواجهة (Frontend) مع السيرفر (Backend) لتسهيل النشر.

### خيارات السيرفر (مجاني/رخيص)
بما أن التطبيق يحتاج إلى قوة معالجة (Python + PDF Processing)، الاستضافات العادية لا تكفي. تحتاج إلى **VPS** أو **Container Platform**.

| الخيار | التكلفة | الأداء | الملاحظات |
| :--- | :--- | :--- | :--- |
| **Render.com** | مجاني (مؤقتاً) | جيد | يتوقف بعد عدم الاستعمال (Spin down). ممتاز للتجربة. |
| **Railway.app** | 5$ رصيد تجريبي | ممتاز | أسهل خيار للبدء (يدعم Dockerfile تلقائياً). |
| **Hetzner** | ~5$/شهر | خارق | سيرفر كامل (VPS) في ألمانيا. أرخص وأقوى خيار احترافي. |
| **DigitalOcean** | 4-6$/شهر | ممتاز | خيار مشهور ومستقر. |

### طريقة الرفع (باستخدام Docker)
لقد جهزت لك ملف `Dockerfile`. فقط ارفع المشروع إلى GitHub ثم اربطه بـ Render أو Railway، وسيقوم بالعمل تلقائياً.

---

## ثانياً: تطبيق الأندرويد (APK) 📱
التطبيق جاهز كملف مشروع أندرويد (Android Project).

### الخطوات لإنشاء ملف APK:
1.  حمل برنامج **Android Studio** (مجاني من جوجل).
2.  افتح المجلد التالي داخل البرنامج:
    `d:\app_file\translat\frontend\android`
3.  انتظر قليلاً حتى يقوم البرنامج بتحميل المكتبات (Gradle Sync).
4.  من القائمة العلوية: اضغط **Build > Build Bundle(s) / APK(s) > Build APK**.
5.  سيظهر لك ملف الـ APK جاهزاً للتثبيت على هاتفك!

### تنبيه هام جداً للإتصال ⚠️
التطبيق على الهاتف يحتاج للاتصال بالسيرفر.
1.  افتح ملف `frontend/src/App.jsx`.
2.  عدل المتغير `API_BASE` في السطر 30 تقريباً.
3.  ضع رابط السيرفر الخاص بك (مثلاً `https://my-app.onrender.com`).
4.  قم بعمل `npm run build` و `npx cap sync` مرة أخرى لتحديث الكود في الأندرويد.
